using UnityEngine;
using System.Collections;

public class PowerUp : MonoBehaviour {

    public enum PowerUpTypes {
        REGENERATION = 0, TURBO = 1, MEGAPROTECTION = 2, VAMPIBOT = 3, QUICKFIRE = 4, COUNT = 5
    }

    // NOTE: For the moment, this class is only used for the enumerator.

}
