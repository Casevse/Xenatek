using UnityEngine;
using System.Collections;

public class Globals : MonoBehaviour {

    // NOTE: By default, const is static.
    public const float ROOM_SIZE = 12.0f;
    public const float HALF_ROOM_SIZE = ROOM_SIZE / 2.0f;
    public const float QUARTER_ROOM_SIZE = ROOM_SIZE / 4.0f;
    public const float WALL_OFFSET = 5.75f;     // For DEBUG.
    public const float NUMBER_OFFSET = 6.0f;    // For DEBUG.

}
